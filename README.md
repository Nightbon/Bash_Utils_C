# README

## Simple Bash Utilities

Реализация утилит `cat` и `grep` для UNIX-подобных систем. Проект выполнен в рамках учебного задания для отработки навыков системного программирования на языке C.

---

## 📋 Содержание
- [Описание проекта](#описание-проекта)
- [Сборка и установка](#сборка-и-установка)
- [Использование](#использование)
  - [Утилита cat](#утилита-cat)
  - [Утилита grep](#утилита-grep)
- [Тестирование](#тестирование)
- [Структура проекта](#структура-проекта)
- [Требования](#требования)
- [Лицензия](#лицензия)

---

## 📝 Описание проекта

Проект представляет собой реализацию двух базовых утилит UNIX:

1. **s21_cat** — аналог стандартной утилиты `cat` для чтения и объединения файлов.
2. **s21_grep** — аналог стандартной утилиты `grep` для поиска по регулярным выражениям.

**Ключевые особенности:**
- Полное соответствие POSIX-спецификации для реализованных флагов
- Обработка бинарных файлов
- Поддержка Unicode (UTF-8)
- Корректное управление памятью (проверено Valgrind)
- Модульная архитектура с переиспользуемыми компонентами

---

## 🔧 Сборка и установка

### Требования
- GCC (рекомендуется версия 9.0 или новее)
- Make
- POSIX-совместимая ОС (Linux, macOS, WSL)

### Сборка

```bash
# Клонирование репозитория
git clone <url-репозитория>
cd src

# Сборка cat
cd cat
make s21_cat

# Сборка grep
cd ../grep
make s21_grep
```

### Очистка

```bash
make clean
```

---

## 🚀 Использование

### Утилита cat

```bash
./s21_cat [OPTION]... [FILE]...
```

**Поддерживаемые флаги:**

| Флаг | Длинная версия | Описание |
|------|----------------|----------|
| `-b` | `--number-nonblank` | Нумерует только непустые строки |
| `-n` | `--number` | Нумерует все строки |
| `-s` | `--squeeze-blank` | Сжимает несколько пустых строк в одну |
| `-e` | — | Отображает `$` в конце строк + `-v` |
| `-E` | — | Отображает `$` в конце строк |
| `-t` | — | Отображает табуляции как `^I` + `-v` |
| `-T` | — | Отображает табуляции как `^I` |

**Примеры:**
```bash
# Нумерация строк с подавлением пустых
./s21_cat -b file.txt

# Отображение управляющих символов
./s21_cat -e -t file.txt

# Сжатие пустых строк
./s21_cat -s file.txt
```

---

### Утилита grep

```bash
./s21_grep [OPTION]... PATTERN [FILE]...
./s21_grep [OPTION]... -e PATTERN... [FILE]...
./s21_grep [OPTION]... -f PATTERN_FILE... [FILE]...
```

**Поддерживаемые флаги:**

| Флаг | Описание |
|------|----------|
| `-e` | Шаблон для поиска (можно использовать несколько раз) |
| `-i` | Игнорирует регистр символов |
| `-v` | Инвертирует поиск (выводит строки НЕ совпадающие с шаблоном) |
| `-c` | Выводит только количество совпадающих строк |
| `-l` | Выводит только имена файлов с совпадениями |
| `-n` | Выводит номера строк |
| `-h` | Не выводит имена файлов |
| `-s` | Подавляет сообщения об ошибках |
| `-f` | Читает шаблоны из файла |
| `-o` | Выводит только совпадающие части строк |

**Примеры:**
```bash
# Поиск с игнорированием регистра
./s21_grep -i "hello" file.txt

# Количество совпадений в нескольких файлах
./s21_grep -c "error" log1.txt log2.txt

# Поиск по нескольким шаблонам
./s21_grep -e "error" -e "warning" log.txt

# Вывод только совпавших частей
./s21_grep -o "[0-9]+" data.txt
```

---

## 🧪 Тестирование

Проект содержит комплексный набор тестов, проверяющих:

### Функциональное тестирование
- Корректность работы каждого флага
- Комбинации флагов
- Краевые случаи (пустые файлы, бинарные данные, длинные строки)
- Обработка ошибок

### Статический анализ
```bash
cppcheck --enable=all --suppress=missingIncludeSystem s21_cat.c
cppcheck --enable=all --suppress=missingIncludeSystem s21_grep.c
```

### Форматирование кода
```bash
clang-format -n *.c *.h
```

### Проверка памяти
```bash
valgrind --leak-check=full ./s21_cat test_file.txt
valgrind --leak-check=full ./s21_grep pattern test_file.txt
```

### Запуск тестов

```bash
# Для cat
cd src/cat
make test

# Для grep
cd src/grep
make test
```

**Структура тестовых данных:**
- `test_data/` — генерируемые тестовые файлы
- `expected/` — эталонные выводы от стандартных утилит
- `output/` — выводы тестируемых программ

---

## 📁 Структура проекта

```
src/
├── common/                 # Общие модули
│   ├── error.c            # Обработка ошибок
│   ├── error.h
│   ├── error_codes.h     # Коды возврата
│   ├── is_binary_file.c  # Определение бинарных файлов
│   └── is_binary_file.h
│
├── cat/                   # Утилита cat
│   ├── Makefile
│   ├── run_tests.sh      # Скрипт тестирования
│   ├── s21_cat.c
│   └── s21_cat.h
│
└── grep/                  # Утилита grep
    ├── Makefile
    ├── run_tests.sh      # Скрипт тестирования
    ├── s21_grep.c
    └── s21_grep.h
```

---

## ⚙️ Требования

### Функциональные требования
- Поддержка всех указанных флагов в соответствии с POSIX
- Корректная обработка стандартного ввода (`-` или отсутствие файла)
- Правильное управление памятью (без утечек)
- Обработка бинарных файлов

### Технические требования
- Язык: C (стандарт C11)
- Стиль кодирования: Google Style
- Отсутствие `goto`
- Один выход из функций (исключение — проверка аргументов)
- Вложенность блоков ≤ 4
- Размер функций ≤ 40-50 строк

---

<div align="center">
<i>Учебный проект</i><br>
</div>